
import { MonoBehaviour, Input, KeyCode } from "../../engine/qbcreates-js-engine.js";

export default class SnakeTesttest extends MonoBehaviour{

    awake() {
    }

    start() {
    }

    update() {
    }
}