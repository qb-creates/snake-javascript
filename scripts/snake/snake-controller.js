import { MonoBehaviour } from "../../engine/qbcreates-js-engine.js";

export default class SnakeController extends MonoBehaviour{
    constructor(snake) {
        this.snakeMovement = snake;
    }

    Start() {
        
    }

    FixedUpdate() {

    }
}